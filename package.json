{
  "name": "quiet-desk",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,app -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix app\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
